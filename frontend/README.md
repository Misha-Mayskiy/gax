# GAX Messenger Frontend

Полнофункциональный фронтенд для мессенджера GAX с интеграцией всех сервисов.

## 🚀 Быстрый старт

### Предварительные требования
- Node.js 16+
- API Gateway (запущенный на localhost:8080)

### Установка
```bash
# Клонирование репозитория
git clone <repository-url>
cd gax-messenger-frontend

# Установка зависимостей
npm install

# Настройка переменных окружения
cp .env.example .env.local

# Редактируйте .env.local при необходимости

# Запуск в режиме разработки
npm start